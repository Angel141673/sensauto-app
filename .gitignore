node_modules
.next
.env.local
.env*.local
.DS_Store

# Copias de respaldo duplicadas (no forman parte de la app real)
*\ copia
*\ copia.*

# Paquetes comprimidos de entrega/backup (no son código fuente)
*.zip

# Artefacto local de `tsc --noEmit` incremental
tsconfig.tsbuildinfo
